# ──────────────────────────────────────────────
# Night-Journal — 环境变量模板
# 复制为 .env 并填写真实值后启动
# ──────────────────────────────────────────────

# JWT 签名密钥（必填，至少 32 位随机字符串）
# 生成方式：openssl rand -hex 32
APP_SECRET=

# SQLite 数据库文件位置（可选）
# 留空则默认 ./data/night-journal.sqlite
# 也支持 file:./data/night-journal.sqlite 的写法
DATABASE_URL=

# 管理员账号 union_id（可选，留空则无管理员；
# 本地账号格式为 local:<username>）
OWNER_UNION_ID=

# 服务端口（可选，默认 3000）
PORT=3000

# ── 定时日记生成（可选） ────────────────────────
# 开发模式下启用自动日记生成调度器（生产环境默认启用）
# ENABLE_AUTO_GENERATION_IN_DEV=true
